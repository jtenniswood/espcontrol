---
title: 10.1-inch Guition JC8012P4A1
description:
  EspControl on the Guition JC8012P4A1 and newer JC8012P4A1 panel revision - 10.1-inch 1280x800 landscape touchscreens with 20 cards, powered by ESP32-P4.
---

# 10.1-inch JC8012P4A1

The **Guition JC8012P4A1** is a 10.1-inch touchscreen powered by an **ESP32-P4** processor and ESP32-C6 WiFi co-processor. EspControl uses it in landscape orientation, with a 1280x800 layout and room for **20 cards** on the home screen.

There are two known rear-case revisions. They look very similar and the newer one may not say `V2` on the label.

::: warning Confirm production silicon before using case dates
- If ESPHome logs or `esptool` chip information reports **ESP32-P4 v3.x production silicon**, use the **V3 firmware** below regardless of the rear-case date or label.
- Rear case marking `2627` or lower: use **JC8012P4A1 original panel** firmware.
- Rear case marking `2628` or higher: use **JC8012P4A1 new panel** firmware.

Case dates are only a fallback for panels whose chip information does not identify V3 silicon.
:::

## Card Grid

Both 10.1-inch revisions use the same grid size and card layout.

<!--@include: ../generated/screens/jc8012p4a1-grid.md-->

## Install

Connect the display to your computer with a USB-C data cable. Confirm V3 silicon first; otherwise choose the button that matches the rear case number, then install.

### Original panel: rear case `2627` or lower

<!--@include: ../generated/screens/jc8012p4a1-install.md-->

### New panel: rear case `2628` or higher

<!--@include: ../generated/screens/jc8012p4a1-v2-install.md-->

### V3 panel: ESP32-P4 v3.x production silicon

<!--@include: ../generated/screens/jc8012p4a1-v3-install.md-->

<C6RecoveryCallout
  slug="guition-esp32-p4-jc8012p4a1"
  primary-label="Recover original panel (rear case 2627 or lower)"
  secondary-slug="guition-esp32-p4-jc8012p4a1-v2"
  secondary-label="Recover new panel (rear case 2628 or higher)"
/>

For a V3 panel, select **JC8012P4A1 V3 production-silicon panel** in the [C6 recovery selector](/getting-started/c6-recovery?device=guition-esp32-p4-jc8012p4a1-v3), regardless of case date.

For a full walkthrough including WiFi setup and Home Assistant pairing, see the [Install guide](/getting-started/install).

## ESPHome Manual Setup

If you use ESPHome and prefer to compile firmware yourself, confirm production silicon first. Use the V3 package for ESP32-P4 v3.x; use the case-date package only when V3 silicon is not present.

### Original panel: rear case `2627` or lower

```yaml
substitutions:
  name: "kitchen-screen"
  friendly_name: "Kitchen Screen"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  setup:
    url: https://github.com/jtenniswood/espcontrol/
    file: devices/guition-esp32-p4-jc8012p4a1/packages.yaml
    refresh: 1sec
```

### New panel: rear case `2628` or higher

```yaml
substitutions:
  name: "kitchen-screen"
  friendly_name: "Kitchen Screen"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  setup:
    url: https://github.com/jtenniswood/espcontrol/
    file: devices/guition-esp32-p4-jc8012p4a1-v2/packages.yaml
    refresh: 1sec
```

### V3 panel: ESP32-P4 v3.x production silicon

```yaml
substitutions:
  name: "kitchen-screen"
  friendly_name: "Kitchen Screen"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  setup:
    url: https://github.com/jtenniswood/espcontrol/
    file: devices/guition-esp32-p4-jc8012p4a1-v3/packages.yaml
    refresh: 1sec
```

## Manual USB fallback

If the browser installer cannot communicate with the panel, install the release image manually:

1. Open the [latest EspControl release](https://github.com/jtenniswood/espcontrol/releases) and download `guition-esp32-p4-jc8012p4a1-v3.factory.bin` from its **Assets**. Do not use the OTA image, a recovery image, or the source archive.
2. Confirm the chip is ESP32-P4 v3.x production silicon. Put the panel into download mode using its **BOOT/download** control: unplug it, hold BOOT while reconnecting the P4 USB programming cable, then release BOOT when the serial port appears.
3. Flash the downloaded **merged factory image** to the P4 serial port at address `0x0` with [esptool](https://docs.espressif.com/projects/esptool/en/latest/esp32p4/esptool/basic-commands.html), for example:

   ```bash
   esptool --chip esp32p4 --port YOUR_SERIAL_PORT write-flash 0x0 guition-esp32-p4-jc8012p4a1-v3.factory.bin
   ```

4. Reset or power-cycle the panel after the write completes. The first boot may take up to 90 seconds to create the `ESP_xxxxxx` setup Wi-Fi network, where `xxxxxx` is the last six characters of the P4 MAC address.
5. Join that network and open `http://192.168.4.1` to enter Wi-Fi details, then complete Home Assistant pairing.

This is browser **USB installation**; it is separate from browser OTA. V3 deliberately rejects browser firmware uploads with HTTP 403. Once the panel is online, use native ESPHome OTA for later updates. If the hosted editor is blank or does not save settings, use the [embedded-editor workaround](/getting-started/troubleshooting#web-configuration-changes-won-t-save) with `?espcontrol_fallback=1`.

Migrating from the V3 experimental firmware: export **Settings > Backup** first. USB flashing may replace the partition layout and erase stored configuration; restore the backup after Wi-Fi and Home Assistant are working.

## Where to Buy

- **AliExpress:** [~£40](https://s.click.aliexpress.com/e/_c4W6TYvp)
- **Desk stand** (3D printable): [MakerWorld](https://makerworld.com/en/models/2490049-guition-p4-10inch-screen-stand#profileId-2736046)
- **Wall mount** (3D printable): [MakerWorld](https://makerworld.com/en/models/2991675-wall-mount-for-10-1-inch-guition-esp32-jc8012p4a1#profileId-3357939)
