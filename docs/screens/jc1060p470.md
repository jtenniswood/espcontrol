---
title: 7-inch Guition JC1060P470
description:
  EspControl on the Guition JC1060P470 and the newer JC1060P470 panel revision — 7-inch 1024x600 landscape touchscreens with 15 cards, powered by ESP32-P4.
---

# 7-inch JC1060P470

![EspControl on the 7-inch JC1060P470: home screen with temperature, clock, and control tiles](/images/jc1060p470-hero.jpg)

The **Guition JC1060P470** is a 7-inch landscape touchscreen powered by an **ESP32-P4** processor. It has a 1024×600 display and room for **15 cards** on the home screen.

Guition ships two 7-inch panels under the same JC1060P470 name. They look identical, so check the label on the back of the case before you flash.

::: warning Choose the firmware by the rear label
- No `V2` in the SKU / material number: use **JC1060P470 original panel** firmware.
- `V2` in the SKU / material number: use **JC1060P470 new panel** firmware.

The wrong profile leaves a white screen with a vertical noise band. Reflash the other profile over USB to recover.
:::

## Card Grid

Both 7-inch revisions use the same grid size and card layout.

![JC1060P470 card grid showing 15 configurable tiles](/images/jc1060p470-buttons.png)

<!--@include: ../generated/screens/jc1060p470-grid.md-->

## Install

Connect the display to your computer with a USB-C data cable plugged into the port shown below, choose the button that matches the rear label, then install.

![USB-C flashing port on the back of the JC1060P470](/images/jc1060p470-flash-port.png)

### Original panel: no `V2` on the rear label

<!--@include: ../generated/screens/jc1060p470-install.md-->

### New panel: rear label marked `V2`

<!--@include: ../generated/screens/jc1060p470-v2-install.md-->

<C6RecoveryCallout
  slug="guition-esp32-p4-jc1060p470"
  primary-label="Recover original panel (no V2 on the rear label)"
  secondary-slug="guition-esp32-p4-jc1060p470-v2"
  secondary-label="Recover new panel (rear label marked V2)"
/>

For a full walkthrough including WiFi setup and Home Assistant pairing, see the [Install guide](/getting-started/install).

## ESPHome Manual Setup

If you use ESPHome and prefer to compile firmware yourself, use the package file that matches the rear label.

### Original panel: no `V2` on the rear label

```yaml
substitutions:
  name: "office-screen"
  friendly_name: "Office Screen"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  setup:
    url: https://github.com/jtenniswood/espcontrol/
    file: devices/guition-esp32-p4-jc1060p470/packages.yaml
    refresh: 1sec
```

### New panel: rear label marked `V2`

```yaml
substitutions:
  name: "office-screen"
  friendly_name: "Office Screen"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  setup:
    url: https://github.com/jtenniswood/espcontrol/
    file: devices/guition-esp32-p4-jc1060p470-v2/packages.yaml
    refresh: 1sec
```

For the Ethernet model, use the advanced manual setup option instead. The package path below is the original panel; boards whose rear label is marked `V2` must swap in the `-v2` path shown in the comment, or the display comes up white with a vertical noise band.

```yaml
substitutions:
  name: "office-screen"
  friendly_name: "Office Screen"
  network_transport: ethernet
  disable_updates: "true"

packages:
  setup:
    url: https://github.com/jtenniswood/espcontrol/
    # New panel (rear label marked V2):
    #   file: devices/guition-esp32-p4-jc1060p470-v2/packages.yaml
    file: devices/guition-esp32-p4-jc1060p470/packages.yaml
    refresh: 1sec
```

This Ethernet option is for advanced manual installs only. It builds an Ethernet-only firmware: WiFi, captive portal, the WiFi setup hotspot, and Bluetooth proxy are not included, and the ESP32-C6 hosted WiFi/Bluetooth co-processor is kept off. The Ethernet build also uses a higher backlight PWM frequency on this panel, which avoids the shimmer seen during hardware testing with wired networking enabled. The `disable_updates: "true"` substitution removes EspControl's GitHub firmware update checker and update controls, while leaving ESPHome OTA available for manual installs.

Use USB when switching a display between the WiFi and Ethernet firmware variants. OTA updates can fail during that change because the network transport changes.

For the full manual setup notes, see [Manual Setup](/getting-started/manual-esphome-setup#advanced-ethernet-options).

## Where to Buy

- **AliExpress:** [~£40](https://s.click.aliexpress.com/e/_c335W0r5)
- **Desk stand** (3D printable): [MakerWorld](https://makerworld.com/en/models/2387421-guition-esp32p4-jc1060p470-7inch-screen-desk-mount#profileId-2614995)
