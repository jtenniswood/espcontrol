# Interface example image sources

These are illustrative AI-generated mockups, not device screenshots or validation evidence. They were generated with the built-in image generation tool on 2026-09-22, using `../webserver-card-gallery.png` as the visual reference. Layout sizes were checked against `devices/manifest.json` and the generated screen-grid documentation at commit `e844c273b`.

Keep the public gallery's mockup disclosure when reusing these assets. The images show invented names and readings; font, icon, spacing and colour rendering may differ from firmware. No configuration backup is included.

## Prompts

### living-room-mockup.png

Use case: ui-mockup. Asset type: EspControl documentation illustrative interface example, NOT a new UI design. Reference image 1 is the existing EspControl web preview; use its exact visual language: black screen, regular grid, small rounded rectangular tiles, flat charcoal #333333 control tiles and #222222 information tiles, white Material Design style icons near upper left, thin white sans-serif large values, small white labels near lower left. Active tiles use flat orange #FF8C00 with white icons and labels. No gradients, shadows, decoration, device bezel, photography, browser chrome, outer labels, branding, watermark, extra controls, or editor plus buttons. All tile content left aligned, consistent padding. Narrow black top status bar has a small temperature at left, time centered, white wifi icon at right. Create a crisp front-on interface mockup that fills the whole image. Exact readable text. Landscape image aspect ratio 1024:600. Exactly FIVE equal grid columns and THREE equal rows beneath the status bar. Top bar: "21°" left, "19:30" centre. Row 1: column 1 orange bulb tile "Living Room"; column 2 charcoal bulb tile "Kitchen"; column 3 charcoal climate tile with large "21" and small "°C", label "Idle"; columns 4-5 merged as ONE wide information tile with large "Evening Mix", label "Living Room". Row 2 five separate tiles: charcoal blinds icon "Blinds"; charcoal closed padlock "Front Door"; charcoal clapperboard play icon "Movie"; information large "18/10" small "°C" label "Today"; information large "21.4" small "°C" label "Room". Row 3 five separate tiles: information large "46" small "%" label "Humidity"; information large "420" small "W" label "Power"; charcoal speaker icon "Volume"; charcoal folder icon "Lights" with a small white chevron at top right; information large "22" label "September". Exactly 14 cards occupying 15 slots, no other text. Match the reference's proportions and uncluttered utilitarian look.

### hallway-mockup.png

Use case: ui-mockup. Asset type: EspControl documentation illustrative interface example, NOT a new UI design. Reference image 1 is the existing EspControl web preview; use its exact visual language: black screen, regular grid, small rounded rectangular tiles, flat charcoal #333333 control tiles and #222222 information tiles, white Material Design style icons near upper left, thin white sans-serif large values, small white labels near lower left. Active tiles use flat orange #FF8C00 with white icons and labels. No gradients, shadows, decoration, device bezel, photography, browser chrome, outer labels, branding, watermark, extra controls, or editor plus buttons. All tile content left aligned, consistent padding. Narrow black top status bar has a small temperature at left, time centered, white wifi icon at right. Create a crisp front-on interface mockup that fills the whole image. Exact readable text. Portrait image aspect ratio 480:800. Exactly TWO equal grid columns and THREE equal rows beneath the narrow top status bar. Top bar: "20°" left, "08:15" centre. Row 1: left orange bulb tile label "Hall Light"; right charcoal closed padlock tile label "Front Door". Row 2: left orange open-door icon tile label "Patio"; right information charcoal tile with simple standing person icon label "Hall". Row 3: left information tile with large "12" followed by small "°C" and label "Outside"; right charcoal clapperboard play icon tile label "Away". Exactly six cards. No security status messages, no sliders, no extra text. Match the reference's font, spacing and small corner radius, adapted to the tall 2-column grid.

### bedside-mockup.png

Use case: ui-mockup. Asset type: EspControl documentation illustrative interface example, NOT a new UI design. Reference image 1 is the existing EspControl web preview; use its exact visual language: black screen, regular grid, small rounded rectangular tiles, flat charcoal #333333 control tiles and #222222 information tiles, white Material Design style icons near upper left, thin white sans-serif large values, small white labels near lower left. Active tiles use flat orange #FF8C00 with white icons and labels. No gradients, shadows, decoration, device bezel, photography, browser chrome, outer labels, branding, watermark, extra controls, or editor plus buttons. All tile content left aligned, consistent padding. Narrow black top status bar has a small temperature at left, time centered, white wifi icon at right. Create a crisp front-on interface mockup that fills the whole image. Exact readable text. Square image aspect ratio 1:1. Exactly THREE equal grid columns and THREE equal rows beneath narrow top status bar. Top bar: "19°" left, "22:15" centre. Row 1: columns 1-2 merged as ONE wide information tile, large left-aligned time "22:15" and no label below; column 3 charcoal bulb icon tile label "Lamp". Row 2: left charcoal blinds icon tile "Blinds"; middle charcoal clapperboard play icon tile "Goodnight"; right information tile large "19.5" small "°C", label "Room". Row 3: left information tile large "22" label "September"; middle charcoal speaker icon tile "Volume"; right charcoal folder icon tile "Lights", with small white chevron at upper right. Exactly eight cards occupying nine slots. No bottom navigation, no alarm controls, no switches, no extra text. Match the reference's white text sizes and practical grid.

## Final revisions

The user selected the hallway example as the padding reference. The final living-room asset uses smaller icons, and both living-room and bedside assets put the subpage chevron on the bottom label row. The hallway asset is unchanged. These edits supersede the corresponding placement instructions in the initial prompts above.

### Living-room icon sizing

Use case: precise-object-edit. Edit target: the attached EspControl living-room interface mockup. User correction: the card icons are too large. Reduce the dimensions of ALL SEVEN main card pictogram icons to 65% of their current width and height (35% smaller): the two lightbulbs, Blinds icon, Front Door padlock, Movie clapperboard, Volume speaker, and Lights folder. Keep each icon anchored at its existing upper-left position inside the card, with the same left and top inset; do not centre it. The remaining space becomes the existing card background. Use consistent optical size of roughly 52–60 pixels high at this image's 1639-pixel width. IMPORTANT: preserve absolutely all text, large numeric values, time, units, labels, their font sizes, and their positions exactly. Preserve the exact grid, card sizes, gaps, card backgrounds, orange active tile, image aspect ratio, and total composition. Do not resize the whole image or the tiles. Keep the top-bar wifi symbol and the Lights chevron unchanged. No added content, no extra styling. Only shrink the seven card pictograms.

### living-room padding and chevron

Use case: precise-object-edit. Image 1 is the interface edit target. Image 2 is the approved hallway mockup, a PADDING REFERENCE ONLY; do not copy its content or portrait layout. Correct the target's internal card padding to match the reference's proportions: labels sit closer to the bottom and slightly closer to the left edge, with about 7.5% of a single tile's width as left text padding and 10% of tile height as bottom padding beneath text. Icons start about 14% of tile height below the top edge, with consistent optical left alignment. Apply those same insets to every card; a wide card has the same absolute inset as a single card, never double padding. Keep all text values and typography sizes. CRITICAL second correction: move the Lights subpage chevron from the upper-right to the BOTTOM-RIGHT, aligned vertically with the centre of the word "Lights", with a right inset matching the label's left inset. There must be no chevron remaining at the top. Preserve all labels and readings, colors, card positions, grid shape, outer dimensions, header, icons' identities. No extra UI. This is the landscape 5-column by 3-row target. Preserve the SMALL icon sizes already present in image 1 (about 52–60 pixels high). Do not enlarge them. Label left inset about 23 px; label bottom inset about 26 px. Icons' top inset about 36 px. All text must remain identical. The "Evening Mix" card continues to span columns 4–5. Only adjust content padding and relocate the Lights chevron.

### bedside padding and chevron

Use case: precise-object-edit. Image 1 is the interface edit target. Image 2 is the approved hallway mockup, a PADDING REFERENCE ONLY; do not copy its content or portrait layout. Correct the target's internal card padding to match the reference's proportions: labels sit closer to the bottom and slightly closer to the left edge, with about 7.5% of a single tile's width as left text padding and 10% of tile height as bottom padding beneath text. Icons start about 14% of tile height below the top edge, with consistent optical left alignment. Apply those same insets to every card; a wide card has the same absolute inset as a single card, never double padding. Keep all text values and typography sizes. CRITICAL second correction: move the Lights subpage chevron from the upper-right to the BOTTOM-RIGHT, aligned vertically with the centre of the word "Lights", with a right inset matching the label's left inset. There must be no chevron remaining at the top. Preserve all labels and readings, colors, card positions, grid shape, outer dimensions, header, icons' identities. No extra UI. This is the square 3-column by 3-row target. Keep the wide clock in row 1 columns 1–2. At this resolution labels should have about 30 px left inset and 37 px below the text, including Lamp, Blinds, Goodnight, Room, September, Volume, Lights. Current bottom label padding is too big: lower the labels. Icons should start roughly 52 px below the card top. Match the reference's orderly padding, without scaling up any icons or text. Clock and sensor values retain their current size. Lights chevron belongs on the bottom label row at the far right.
