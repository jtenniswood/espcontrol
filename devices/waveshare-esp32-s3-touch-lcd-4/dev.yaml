# =============================================================================
# DEV CONFIG - Local development (Waveshare ESP32-S3-Touch-LCD-4)
# =============================================================================
# Flash with: python3 scripts/local_esphome.py devices/waveshare-esp32-s3-touch-lcd-4/dev.yaml run
# =============================================================================

substitutions:
  name: "espcontrol-wslcd4"
  friendly_name: "EspControl Waveshare LCD4"
  espcontrol_component_url: "../.."
  espcontrol_component_ref: "HEAD"

wifi:
  ssid: !secret wifi_ssid
  password: !secret wifi_password

packages:
  espcontrol: !include packages.yaml

web_server:
  js_url: ""
  js_include: "../../docs/public/webserver/waveshare-esp32-s3-touch-lcd-4/www.js"
